pip install -U langchain langsmith
pip install -U openai # (OpenAI 모델을 사용할 경우)
pip install -U langchain-openai # 


# 추가로 사용하는 LLM 공급자의 API Key도 여기에 설정 


LangSmith로 볼 수 있는 것
Prompt
Response
입력/출력 토큰 수
비용(모델에 따라)
실행 시간
호출 순서
LangGraph의 각 노드 실행 과정
오류 발생 위치

즉, "AI 프로그램의 디버거"라고 생각하면 됩니다.
